# 할 일

2026-09-08 리뷰에서 나온 것들. 우선순위 순서다.
전체 맥락은 [AGENTS.md](AGENTS.md)에 있다.

## 추가 집중 점검 · 수정 완료 (2026-09-09)

근거와 재현 결과는 [수집·집계·감시 점검 보고서](PIPELINE_REVIEW_2026-09-09.md)에 있다.

- P1: 시간봉 집계 하한을 정시로 맞추고, 오래된 백필의 집계와 원본 삭제 전 완전성을 보장한다. 원본이 남은 불일치 봉을 복구한다.
- P1: 매물 400개 응답의 경계 가격을 소진 판정에서 제외하고, 기존 잘못된 소진 기록을 정정한다.
- P1: 일부 아이템만 멈추는 부분 장애도 감지·알림한다.
- P2: 복구와 알림 기록을 분리해 쿨다운을 지키고, 지속 수집 실패를 종료 코드에 반영한다. 시간봉 상태 대조 테스트는 읽기 전용으로 분리한다.
- 분석 페이지: 정합성 확인 후 재계산하고, 소진량 검증 상태와 실제 수집·집계 시각을 표시한다.

위 항목을 구현하고 운영 DB에 적용했다. 초기 복구 후 3,375봉 대조 불일치 0,
재관측으로 반증된 소진 569건 무효화. 과거 소진 오판을 모두 확정할 수 없어 분석 페이지의
소진 합계는 숨긴다. `npm test`, `npm run test:pipeline`, `npm run test:candles` 및 실제 빌드 통과.
원본이 이미 일부 삭제된 과거 구간의 완전성은 보장하지 않는다.

### 후속 안정화 (2026-09-09)

- 동일 아이템의 수동/Actions 동시 수집을 API 조회 전부터 저장·실패 기록까지 DB 세션 잠금으로 직렬화했다.
- 전체 집계가 90분을 초과하거나 기록이 없으면 별도 감시·health 실패·새 분석 배포 중단·화면 안내로 드러낸다. 감시는 5분, 알림 쿨다운은 6시간이다.
- 실제 DB 연결 간 잠금 경쟁과 임시 테이블의 집계 지연·쿨다운·회복 검증을 통과했다.
- 정기 실행 관찰 중, 집계와 수집의 커밋이 겹치면 검사 대상에 아직 미집계 체결이 섞여 배포를 차단하는 문제를 발견했다. 집계에 포함한 최대 체결 ID를 기록하고, 이후 체결은 다음 집계 대기로 분리했다. 운영 3,403봉 대조 불일치 0과 실제 빌드·브라우저 검증을 통과했다.
- 정기 실행 확인: 오늘 13:41 KST 보존과 이후 집계·Pages 배포의 실제 실행 결과는 14:15 KST 후속 점검에서 확인한다. 수동 실행으로 이 검증을 대신하지 않는다.

---

## P1 · 완료 (2026-09-08)

### 1. `events.yml`이 GitHub `schedule`을 쓴다

**문제.** `.github/workflows/events.yml`이 `cron: '30 1 * * 4'`로 걸려 있다.
GitHub의 schedule 트리거는 이 저장소에서 실행을 만들어주지 않는다는 것이
이미 확인됐다(2026-09-07, 40회 넘는 기회 중 1회). 수집기는 그래서 pg_cron으로
옮겼는데 이벤트 동기화는 그대로 남았다.

주 1회라 안 떠도 **몇 주 동안 아무도 모른다.** 실행 이력에도
`workflow_dispatch` 1건뿐이다.

**할 일.** `sql/scheduler.sql`과 같은 방식으로 pg_cron에서 dispatch한다.

- `events.yml`에서 `schedule:` 블록 제거, `workflow_dispatch:`만 남긴다
- `sql/scheduler.sql`에 cron job 추가 (주 1회, 목요일 01:30 UTC 근처)
- Vault의 `gh_dispatch_token`을 그대로 쓴다 (이미 `actions:write` 있음)
- `collect.yml` 상단 주석에 왜 pg_cron을 쓰는지 적어뒀으니 같은 톤으로

**검증.** `SELECT jobname, schedule FROM cron.job;` 에 새 job이 보이고,
수동으로 그 SQL을 실행했을 때 `net._http_response`에 204가 찍힌다.

---

## P2 · 완료 (2026-09-08)

### 2. §02 요일 효과 — "목요일만 유의"는 이제 거짓

**문제.** `web/analysis.html` §02 본문이 이렇게 단정한다:

> 목요일만 유의하게 낮습니다 — 나머지 6일은 전부 ±1.5% 안이고 통계적으로 유의하지 않습니다.

종목이 통째로 바뀌고(잡템 제거, 종결 카드 34종 추가) 체결이 두 배가 되면서
결과가 달라졌다. 2026-09-08 기준 실제 값:

| 요일 | 주간평균 대비 | t | 유의 |
|---|---|---|---|
| 토 | **+6.92%** | 2.02 | ** |
| 금 | +2.22% | 0.18 | |
| 일 | +1.40% | −0.18 | |
| 화 | +0.65% | −0.40 | |
| 수 | −2.47% | −1.24 | |
| 월 | −2.77% | −1.49 | |
| 목 | **−5.95%** | −3.03 | ** |

토요일이 새로 유의해졌고 ±1.5%를 벗어난 날이 5일이다. 두 문장 다 사실이 아니다.

**할 일.** 목요일 단독 서술을 **목요일·토요일 두 축**으로 다시 쓴다.

이건 나쁜 소식이 아니라 가설이 하나 더 검증된 것이다. 원래 계획에
*"토요일 레이드 초기화 → 주말 2차 파동"* 가설이 있었고, 그게 이제 잡혔다.
다만 방향이 예상과 반대다 — 공급 증가(가격↓)가 아니라 **가격 상승**이다.
레이드 준비 수요가 공급을 넘어선다는 해석이 가능하다.

목요일(공급 충격, 가격↓+거래량↑)과 토요일(수요 충격)이 **주간 사이클의
양 끝**을 이룬다는 구조로 쓰면 §07의 4분면 논리와도 이어진다.
거래량 수치를 함께 붙일 것.

**주의.** 숫자를 본문에 하드코딩하지 말 것. 지금도 `summary.json`에서
읽어 채우는 부분이 있으니 같은 방식으로. 데이터가 또 바뀌면 또 틀린다.

### 3. §05 한계 — 수집 공백을 안 밝히고 있다

**문제.** 페이지에 적힌 어떤 한계보다 큰 문제가 빠졌다.
2026-09-07 실측에서 6시간 구간의 **37개 중 21개(57%)가 수집 공백**이었다.
그 구간의 일봉 VWAP은 하루 전체가 아니라 "수집된 몇 시간"의 평균이다.

이건 요일 효과에 시간대 편향을 넣을 수 있고, 백테스트도 낙관적으로 만든다.

**할 일.** §05에 항목을 추가한다. 이제 숫자로 말할 수 있다 —
`collection_health` 테이블이 쌓이고 있으니 가동률을 읽어 표시한다.

```sql
SELECT ROUND(100.0 * COUNT(*) FILTER (WHERE action='ok') / COUNT(*), 2) AS uptime_pct
FROM collection_health WHERE checked_at > now() - interval '24 hours';
```

스케줄러를 pg_cron으로 고친 2026-09-07 15:15 UTC를 기준으로 **이전/이후를
나눠** 보여주면 더 정직하다. `web/build.ts`에서 `summary.json`에 실어 보낼 것.

### 4. §01·§04 고정 숫자가 어긋났다

| 위치 | 본문 | 현재 |
|---|---|---|
| §01 해체 마진 | "±0.3% 안" | **−0.47%** |
| §04 naive 우위 | "7종" | **8종** (커버리지 77%) |

화면에 계산돼 나오는 값은 정확하다. **산문에 박아둔 숫자만** 손보면 된다.
가능하면 이것도 데이터에서 읽게 바꿀 것.

---

## P3 · 완료 (2026-09-08)

### 5. D등급 아이템에 예측을 띄우고 있다

**문제.** 원래 설계는 *"D등급(일평균 체결 5건 미만)에는 예측을 아예 띄우지
않는다"*였는데 지금 띄운다. 예: 종말의 칼날 로페즈(표본 61건, D등급)의
예측이 naive 대비 **−44.6%**다. 현재 D등급 추정 12종.

정직하게 표시는 하지만, 믿을 수 없는 걸 그려놓고 "믿지 마세요"라고 쓰는 것보다
안 그리는 게 낫다.

**할 일.** 둘 중 하나로 정한다.
- (a) 원칙 적용 — `web/build.ts`에서 D등급이면 `forecast`를 만들지 않거나,
  `web/app.js`에서 예측 패널 자체를 숨긴다
- (b) 원칙 폐기 — AGENTS.md와 분석 페이지에서 해당 서술을 지운다

**(a)를 권한다.** 유동성 등급을 "숫자보다 먼저 보여주는" 이 프로젝트의
태도와 (b)는 어긋난다.

---

## P4 · 새 발견을 만들 수 있는 작업

### 6. 카드 34종에 `key_stat` 채우기 — 완료 (2026-09-08)

**왜.** 칭호 4종에 `key_stat`을 채웠더니 발견이 나왔다 —
*최종뎀 9.6%(종이달 오르골) vs 8%(나머지 3종), 가격 차 약 870만 골드.*
**가격 순서가 스탯 순서와 정확히 일치했다.**

카드 34종에 같은 걸 하면 **"부위별 스탯당 골드 단가"**가 나온다.
분석 페이지에 절을 하나 더 만들 수 있는 재료다.

**현재 상태.**

| 카테고리 | 종 | key_stat |
|---|---|---|
| 카드 | 34 | **34 ✅** |
| 강화·증폭 | 11 | **0** |
| 칭호 | 4 | 4 ✅ |

**처리.** `/df/items/{itemId}`의 실제 응답에서 카드 옵션이 든
`cardInfo.enchant`의 최대 업그레이드 단계를 읽어 핵심 스탯을 채웠다.
카드는 "모든 공격력", "최종 데미지 증가", "모든 속성 강화" 등이 부위마다
다르므로, 부위별로 어떤 스탯이 핵심인지 먼저 확인할 것.
채운 뒤 `scripts/init-db.ts`의 SEED도 재생성한다(레포만으로 재현 가능해야 한다).

### 7. 종결 갱신일(`final_since`)이 7종에만 있다 — 완료 (2026-09-08)

칭호 4 · 크리쳐 2 · 오라 1만 있고 **카드 34종은 종결 표시만 있고 갱신일이 없다.**
종결 교체가 가격에 준 충격을 보려면 날짜가 필요하다.
던파 공식 패치 노트에서 카드가 최초 등장한 콘텐츠의 라이브 서버 적용일을
확인해 카드 34종에 모두 입력했다. 시드와 DB 모두 `34/34`이며,
화면의 `종결 D+`도 이 날짜를 기준으로 계산한다.

### 8. 강화·증폭 종결 표시 — 취소 (2026-09-08)

한때 추적 중인 확정 증폭권에서 가장 높은 `+12 장비 증폭권[골고라이언]`에
종결을 표시했지만 잘못된 기준이었다. 증폭은 +13 이상으로도 이어지므로
강화·증폭 카테고리에는 종결 표시를 사용하지 않는다.

---

## P5 · 견고성

### 9. `sync-events.ts` — 적용일 추정이 확정값과 섞인다 — 완료 (2026-09-08)

`appliedDay()`가 제목에 날짜가 없으면 "공지 다음 날"로 추정한다
(주석: *"대규모 업데이트는 통상 수요일 공지, 목요일 적용"*).

**이벤트 스터디는 t=0이 정확해야 의미가 있는데** 추정치가 구분 없이 들어간다.
제목에 적용일이 없어서 공지 다음 날로 계산한 경우 `note`에
`적용일 추정(공지 다음 날)`을 남긴다. 제목에 날짜가 있거나 퍼스트서버 공지인
경우에는 확정값으로 기록한다.

### 10. `sync-events.ts` — 개별 실패가 전체를 죽인다 — 완료 (2026-09-08)

`html()`이 던지면 동기화 전체가 중단된다. 공지 하나가 404여도 **그 주 전체가
실패**한다. 주 1회 실행이라 일주일 공백이 생긴다.

- 업데이트·세리아 상점 상세 페이지를 개별 `try/catch`로 감싸고 실패한 공지만
  제목·URL 오류와 함께 건너뛴다.
- 각 출처의 후보가 0건이면 목록 HTML 구조 확인 경고를 출력한다.

---

## P6 · 모바일 디자인 (2026-09-08 리뷰)

데스크톱과 다크모드는 완성도가 좋다. 아이콘도 68/68 정상이다
(개발 중 "43개 깨짐"으로 보인 건 `loading="lazy"` 때문에 화면 밖 이미지가
`naturalWidth === 0`으로 잡힌 것이지 실제 실패가 아니다. 실제 URL은 전부 200).

**문제는 모바일 목록 화면이다.** 포트폴리오용이라 면접관이 폰으로 열어볼
가능성이 높은데, 지금은 첫 화면에 요약 카드 네 개만 보이고 정작 시세가 안 보인다.

기준 뷰포트: 375×812.

### 11. 모바일 목록이 무너진다 — 완료 (2026-09-08)

`web/style.css`의 `@media (max-width: 780px)` 블록과 `web/app.js`의 행 렌더링.

**(a) 아이템명이 잘려서 못 읽는다.** 현재 그리드가
`28px 120px 92px 84px`인데, 던파 아이템명은 대부분 10자가 넘어
`"숲속의 유..."`로 끝난다. 식별이 안 되면 목록의 의미가 없다.

→ 모바일에서는 **이름·현재가·24h 세 컬럼만** 남긴다. 지금은 4개를 넣으려다
전부 망가졌다. 거래대금·매물·스파크라인은 이미 숨기고 있으니 24h 수량도 뺀다.

**(b) 부제가 3줄로 깨지는데 행 높이는 66px 고정이다.**
`레어 · 표본 289 · A등급`이 줄바꿈되어 서로 부딪힌다.

→ 모바일에서는 부제를 등급 칩 하나로 줄이거나 숨긴다.

**(c) 24h 변동률이 화면 밖으로 잘린다.** `+0…`만 보인다.

375px 이하 목록은 순위와 보조 지표를 숨기고 이름·현재가·24h 세 열만 남겼다.
부제와 목록 태그도 숨겨 긴 이름을 우선 표시하며 행 높이를 줄였다.

### 12. 모바일 헤더가 두 줄로 깨진다 — 완료 (2026-09-08)

브랜드 `던파 경매장`이 `던파 / 경매장`으로, 갱신 시각이
`9. 8. 오전 / 01:27 기준`으로 각각 줄바꿈된다. 헤더 높이가 두 배가 되고
**sticky라 스크롤 내내 화면을 먹는다.**

→ 브랜드에 `white-space: nowrap`. 갱신 시각은 모바일에서 `01:27`만 남기거나 숨긴다.

브랜드 줄바꿈을 막고 모바일 갱신 시각은 숨겼으며 헤더 높이·간격을 줄였다.

### 13. 모바일 요약 카드가 첫 화면을 다 먹는다 — 완료 (2026-09-08)

카드 4개가 세로로 쌓여 **목록을 보려면 스크롤을 네 번** 해야 한다.
시세 사이트인데 시세가 안 보인다.

→ 가로 스크롤 한 줄(`overflow-x:auto` + `scroll-snap`)로 바꾸거나 2×2 그리드로 압축한다.

요약 카드 네 장을 한 줄 가로 스크롤과 스냅 방식으로 바꿨다.

### 14. 탭 그룹 분리가 버그처럼 보인다 — 완료 (2026-09-08)

`크리쳐 / 오라 / 칭호`가 `.tabs-end` div로 묶여 `margin-left:auto`로
오른쪽 끝에 붙어 있다. 종결 슬롯을 묶으려는 의도는 알겠으나 **라벨이 없어
레이아웃이 깨진 것처럼 읽힌다.** 모바일에서는 가로 스크롤 맨 끝이라
존재 자체를 모르게 된다.

→ 구분선이나 작은 라벨(`종결 슬롯`)을 붙이거나, 의도를 포기하고 그냥 이어 붙인다.

오른쪽 그룹 앞에 구분선과 `종결 슬롯` 라벨을 붙였다.

### 15. 빈 패널이 상세 페이지 두 번째 자리를 차지한다 — 완료 (2026-09-08)

`가격·예측` 다음이 `아이템별 요일 프로파일`인데 대부분
*"최소 31일 필요, 현재 30일"*로 비어 있다. 스크롤해야 실제 내용이 나온다.

→ 데이터가 없는 패널은 아래로 내리거나 접어둔다.

요일 프로파일이 아직 준비되지 않았으면 상세 화면 맨 아래로 이동한다.

### 16. 변동률이 없을 때 `- 24시간`으로 나온다 — 완료 (2026-09-08)

`데이터 없음`으로 바꾸거나 그 줄을 숨긴다.

상세 화면에서 변동률이 없으면 `데이터 없음`으로 표시한다.

---

## 확인 방법 (모바일)

브라우저 개발자도구에서 375×812로 맞추고 목록·상세·분석 세 화면을 본다.
가로 스크롤이 생기지 않아야 하고(`document.documentElement.scrollWidth <= innerWidth`),
아이템명이 최소 8자는 읽혀야 한다.

## 하지 않기로 한 것

- **API 키 재발급** — 사용자 판단으로 보류
- **Codex 예약 감시 작업 삭제** — pg_cron 감시견은 감시 대상과 운명을 공유한다
  (pg_cron이 죽으면 같이 죽는다). 빈도만 낮춰 외부 관찰자로 남겨둘 것

---

## P7 · 전체 재리뷰 — 완료 (2026-09-08)

### 17. Actions 수집 출처가 `manual`로 잘못 저장된다

수집 워크플로가 이제 `workflow_dispatch`로만 실행되는데 과거 `schedule` 분기가
남아 있어 Actions 실행도 모두 `manual`이 됐다. 워크플로에서는 출처를 항상
`actions`로 기록하도록 고쳤다. 이미 저장된 과거 행은 실행 주체를 확정할 수 없어
소급 변경하지 않는다.

### 18. 감시견이 실패·중단 실행을 생존 신호로 본다

`MAX(started_at)` 대신 오류가 없고 완료 시각이 있는 마지막 `finished_at`을 본다.
이제 시작 직후 멈춘 실행이나 반복 실패가 있어도 정상 수집으로 오인하지 않는다.

### 19. 사이트 배포도 실패가 확인된 GitHub `schedule`에 의존한다

`pages.yml`의 schedule을 제거하고 `dnf-pages-dispatch`를 pg_cron에 추가했다.
매시 5분 최신 DB로 다시 빌드하며, 예측·카드 최저가 코드 변경도 push 직후
배포되도록 paths를 보완했다.

### 20. 예측 날짜와 학습 데이터가 어긋난다

당일 미완성 일봉을 학습에서 제외하고 예측은 항상 다음 날부터 시작한다.
마지막 거래가 며칠 전이어도 지난 날짜를 예측으로 내보내지 않는다. 중간에
무거래일이 있으면 관측 행 번호가 아니라 실제 달력 일수로 추세를 계산한다.
회귀 테스트를 추가해 이 세 조건을 고정했다.

### 21. 프런트 로딩·접근성 보강

요약/시계열 파일이 실패하면 빈 화면 대신 안내를 보이며, 이전 상세 요청이 늦게
도착해 현재 화면을 덮지 않게 했다. 아이템 행과 카드 슬롯은 실제 링크로 바꿔
키보드 탐색과 새 탭 열기를 지원한다. 목요일 유의성 문구도 최신 t값에 따라 바뀐다.

### 22. 해체 마진이 전체 기간 평균을 사용한다

시세 지표인데 과거 전체 체결을 평균해 시간이 갈수록 현재 가격과 멀어지고 있었다.
패키지와 구성품 5종의 최근 24시간 VWAP으로 통일하고, 여섯 가격 중 하나라도
없으면 불완전한 합계로 차익을 내지 않고 표본 부족으로 표시한다.

### 23. `npm run init`이 포화 대응 주기를 되돌린다

수집기가 100건 포화를 발견해 폴링 주기를 줄여도, 스키마·시드를 다시 적용하면
하드코딩된 초기 주기로 덮어쓰고 있었다. upsert는 기존 운영값과 시드값 중 더 짧은
주기를 보존하도록 바꿨다. 시드로 주기를 더 짧게 조정하는 것은 그대로 반영된다.

---

## 작업 후 확인

```bash
npm run health     # 수집 공백
npm run stats      # 아이템별 커버리지·포화
node --env-file=.env --no-warnings web/build.ts   # 사이트 빌드
```

DB 상태:

```sql
SELECT jobname, schedule, active FROM cron.job;
SELECT action, COUNT(*) FROM collection_health
  WHERE checked_at > now() - interval '24 hours' GROUP BY 1;
SELECT category, COUNT(*), COUNT(key_stat), COUNT(final_since)
  FROM items WHERE tracked GROUP BY 1;
```

---

## P8 · 전체 리뷰 — 완료 (2026-09-08)

### 24. 레전더리 카드 스캔의 조용한 실패

27MB 전체 카탈로그 대신 `data/legendary-cards.json`에 레전더리 카드 165종의 ID와 이름만
커밋했다. 시간별 배포의 실패 숨김을 제거했으며, 로컬 스캔 2회와 DB 적재를 확인했다.

### 25. 레전더리 카드 헤드라인 안정화

단일 최저가 대신 p10을 헤드라인으로 쓰고 최저가·중앙값·매물 종수를 부제로 내렸다.
동률 최저가는 itemId로 정렬해 실행 순서에 따라 카드명이 바뀌지 않게 했다.

### 26. 포화와 주기 단축 신호 분리

100건 포화 기록은 유지하되, 100건의 시간 범위가 현재 주기의 1.5배보다 짧을 때만
주기를 줄인다. 최근 24시간 포화 29건 중 이 조건을 만족한 것은 광휘의 소울 결정 2건이었다.

### 27. 개별 아이템 아사 측정

감시견이 각 아이템의 마지막 성공 시각을 자기 폴링 주기의 5배와 비교한다.
전역 수집이 정상이더라도 지연 아이템이 있으면 `stale_items`로 기록하며,
자가복구와 이슈 생성 조건은 바꾸지 않았다. 분석 페이지는 전역 가동률과 아이템별 신선도를 분리한다.

### 28. 실행 가능한 환경 변수 예제

`.env.example`에 `DATABASE_URL`, `GH_DISPATCH_TOKEN`, 올바른 `SQLITE_PATH`와 발급처·
Session pooler·비밀번호 인코딩 설명을 추가했다. 실제 값은 포함하지 않는다.

### 29. README 동작 설명

자동 단축 조건과 5분 이하 `hot` 그룹 기준을 실제 코드에 맞게 수정했다.

### 30. 정리와 보안

빌드 전에 `dist`를 지워 고아 시계열 파일을 없앤다. 구버전 SQLite 수집기 4개를 종료하고
DB·WAL·SHM을 OneDrive 밖 임시 백업으로 옮겼다. Supabase Root 2021 CA를 번들해 TLS 인증서와
호스트 이름을 검증한다. 레전더리 쿼리는 화면에 필요한 최신 1행만 가져오며, Actions 그룹은
`hot` 8종·`rest` 60종으로 나눠 예상 호출량을 시간당 114회·112회로 맞췄다.

---

## P9 · 분석 표본과 보존 정책 — 완료 (2026-09-08)

### 32. 요일 효과 표본 범위 공개

요일 효과와 같은 조건으로 종목 수·카테고리 구성·완료 일봉 범위·아이템-일을 계산해
`summary.json`에 넣는다. 분석 페이지 §02와 §05는 이 값을 읽으므로 표본이 늘면 자동으로 갱신된다.

### 33. 레전더리 p10과 강화 기대량 설명 수정

p10의 모집단은 165종 전체가 아니라 매물이 확인된 종임을 명시했다. 강화 재료 2.5장은
1장 40%와 2장 80% 전략의 성공당 기대 소모량이며, 3장 100%의 3장보다 작다는 근거를 추가했다.

### 34. 운영 로그 보존 정책

`collection_runs`는 30일, `listing_deltas`는 90일이 지나면 매일 정리한다.
체결 원본 `trades`와 카드 호가 시계열 원본 `listing_snapshots`는 정리하지 않는다.

---

## P10 · 거래대금 TOP100 시험 집계 — 완료 (2026-09-08)

### 35. 스태커블 시장 탐색

카탈로그의 스태커블 11,061종을 하루 한 번 조회한다. 최근 100건이 24시간보다
짧으면 체결 속도로 24시간 거래대금을 환산하고, 화면에서 추정치임을 구분한다.
기존 추적 종목은 DB의 최근 24시간 연속수집 합계로 대체한다.

### 36. 별도 순위 페이지

`ranking.html`에 거래대금 상위 100종을 표시한다. 각 행은 `연속수집`, `24h 조회`,
`24h 환산` 중 산정 방식을 밝히며, 옵션이 섞이는 장비·아바타·카드를 전수 탐색에서
제외한 이유와 API 100건 상한을 함께 설명한다.

---

## P12 · 나중에 할 것 (2026-09-08 기준 보류)

전부 **지금 당장 해야 하는 것이 아니다.** 보류 사유가 항목마다 다르므로 그대로 적어둔다.
"데이터가 부족해서"와 "지금 손댈 단계가 아니라서"는 다른 이유이고, 뒤엣것은 시간이 지나도
저절로 풀리지 않는다.

### 39. 평균회귀 모델 — AR(1)

**2026-09-08 리뷰 정정: 기존 근거를 철회하고 후보 검토로 남긴다.**
빈 시간을 건너뛴 VR과 동분산 검정으로 가격의 평균회귀를 확정할 수 없었다.
`VR(2) − 1`을 가격 수준 AR(1)의 계수로 바로 사용하는 것도 타당하지 않다.

시간축·검정과 기존 모델의 평가를 먼저 바로잡았다. 연속 완료 시간봉, 이분산 견고한
표준오차와 Holm 보정, 날짜별 롤링 평가를 적용했다. 당시 새 검정은 34종 중 11종만
가능했고 보정 후 반전 1종이었다. 일봉 평가는 1일 뒤 13건, 7일 뒤 0건이라 표본이 얇다.
검증은 `npm test`와 실제 데이터 빌드로 하며, 미래 데이터 변경 불변성도 테스트한다.

**후속 모형을 검토할 때 지킬 것**
- 가격 수준 모형과 수익률 모형을 구분하고 계수는 각 학습 구간에서 직접 추정한다.
- 기존 모델·naive와 같은 종목·평가일·예측 거리에서 비교한다.
- 구간 폭 보정은 별도 학습 자료에서 하고, 새 평가 자료에서 커버리지를 확인한다.
- 기존 시간봉 단일 홀드아웃 실험을 새 롤링 평가와 직접 비교하지 않는다.

### 40. 금액으로 환산한 매수 시점 조언

**"지금 사면 얼마를 아낍니다"**라는 조언은 아직 미검증인 후속 과제다.
§01의 분산비만으로 평균회귀나 매수 유리함을 가정하지 않는다.
호가–체결 갭이 싸다는 사실과 실제로 구매 비용을 줄일 수 있다는 주장은 구분한다.

규칙의 별도 백테스트가 필요하다. 수수료·체결 가능 가격과 미래 정보가 없는 평가를 포함한다.

1. 규칙을 정한다 — 예: `갭 ≤ −5%일 때 매수`
2. 과거 데이터에 그 규칙을 적용한다 (`candles_1h` + `listing_snapshots`, 둘 다 영구 보존)
3. **아무 때나 산 것 대비** 평균 몇 % 아꼈는지 계산한다
4. **그 성적을 항상 함께 보여준다**

```
닳아버린 순례의 증표
지금 호가가 24h 평균보다 -10.2% 쌉니다.
과거 이런 상황(갭 ≤ -5%)에서 샀다면 평균 -6.8% 절약
표본 47회 · 아무 때나 샀을 때 대비
```

**4번이 핵심이다.** 조언을 내려면 그 조언의 과거 성적을 같이 내야 한다.
유동성 등급·검정력·환산 배율에 적용한 원칙과 같다.

**주의.** 규칙 임계값(−5%)을 성적이 가장 좋은 값으로 고르면 과최적화다.
임계값을 몇 개 두고 전부 보여주거나, 학습 구간에서 고르고 검증 구간에서 확인한다.

**새로 수집할 데이터는 없다.** 39번과 독립이라 먼저 해도 된다.

### 41. 요일 효과·아이템 유형 절 복귀

2026-09-08 개편에서 **둘 다 분석 페이지에서 내렸다.** 표본이 얇은 채로 결론을 내는 것보다
왜 못 내는지를 한계 절에 적는 편이 낫다고 판단했다.

| | 지금 | 복귀 조건 |
|---|---|---|
| 요일 효과 | 4종·28일 · 최소 탐지 가능 효과 ±4.0% | 종목 20종 이상 → ±2% 아래 |
| 아이템 유형 군집 | 34종 | 종목이 더 늘어 군집이 안정될 때 |

**계산은 `web/build.ts`에 그대로 있다**(`weekday`, `weekdaySample`, `randomWalk.items`의
`category`·`volatility`·`per_bar`). 예측 모델이 요일 계수를 쓰므로 지우면 안 된다.
화면만 다시 붙이면 된다.

**막고 있는 것은 기간이 아니라 종목 수다.** 체결 25건 이상은 34종인데 `>14일 이력`에서
30종이 떨어진다. 100건 상한 탓에 거래가 빠른 아이템일수록 백필이 짧다.
수집 시작이 2026-09-06이므로 **09-21쯤 자동으로 풀린다.**

아이템별 요일 프로파일(완료 일봉 31일 필요, 현재 최장 28일)은 상세 화면에 이미 있고
조건을 채우면 저절로 나타난다.
