# 던파 경매장 시세 추적기 — 작업 인계

던전앤파이터 경매장 시세를 수집·시각화·분석하는 웹서비스. **포트폴리오/면접용**이며 수익화하지 않는다.

- 사이트 <https://kimtaehoon1107-gif.github.io/dnf-price-tracker/>
- 레포 <https://github.com/kimtaehoon1107-gif/dnf-price-tracker>
- 설계 원본 <https://claude.ai/code/artifact/77531858-d214-445f-ad10-8bb3908123ff>

---

## 0. 먼저 알아야 할 한 가지

**Neople 오픈 API는 과거 시세를 주지 않는다.** `/df/auction-sold`는 최근 **100건 또는 최대 1개월** 중 먼저 걸리는 쪽까지만 돌려준다.

따라서 히스토리는 직접 쌓는 수밖에 없고, **이 프로젝트의 본체는 차트가 아니라 수집 파이프라인**이다. UI와 분석은 나중에 만들 수 있지만 수집은 되돌릴 수 없다. 아래 모든 결정이 이 한 문장에서 나온다.

---

## 1. 현재 상태 (2026-09-08 02:14 KST 점검 스냅샷)

| | |
|---|---|
| 추적 아이템 | **68종** |
| 체결 데이터 | **22,136건** · 2026-08-08 ~ 현재 |
| 부수 데이터 | listings 10,310 · deltas 8,273 · snapshots 2,307 |
| 수집 실행 | 1,814회, **실패 0** |
| 예측 생성 | 10종 (완료 일봉 10일 이상·D등급 제외) |

### 추적 종목

| 카테고리 | 종 | 종결 | 폴링 주기 |
|---|---|---|---|
| 카드 | 34 | 34 | 1800~3600s |
| 강화·증폭 | 11 | – | 900~3600s |
| 소울 결정 | 6 | – | 300s |
| 유랑악단 패키지 | 6 | – | 900s |
| 재료·소모품 | 4 | – | 120~900s |
| 칭호 | 4 | 4 | 1800s |
| 크리쳐 | 2 | 2 | 900~3600s |
| 오라 | 1 | 1 | 3600s |

표의 주기는 시드 기본값이다. 100건 포화이면서 그 100건이 현재 주기의 1.5배도
덮지 못할 때만 운영 DB에서 최소 60초까지 자동 단축한다. 수집 공백 때문에 생긴 포화는 기록만 남긴다.

**"종결"** = 현재 기준 최상위 아이템. 패치로 교체되므로 `is_final` / `final_since`로 관리한다.
종결 갱신일: 카드는 최초 등장 콘텐츠별 2022-10-27~2026-08-06,
크리쳐 2026-01-22, 칭호 2026-01-23, 오라 2026-06-04.

---

## 2. 아키텍처

```
Supabase pg_cron ──workflow_dispatch──> GitHub Actions
                                          │
Neople API ──아이템별 주기(src/run.ts)─────┴──> Supabase Postgres
                                                     │ 원본 체결 → 시간봉
                                                     │ 매시간 빌드
                                                     ▼
                                      정적 사이트 ──> GitHub Pages
```

**서버가 0대다.** 조회 트래픽이 사실상 없는데 상시 서버를 굴릴 이유가 없고, 수집 Actions가 어차피 돌기 때문에 그때 같이 구우면 갱신 주기가 같아진다.

- 런타임 Node 24 (TypeScript를 빌드 없이 직접 실행). 의존성은 `pg` 하나
- DB Supabase Postgres (ap-northeast-2). **Session pooler**로 붙는다
- 수집 `.github/workflows/collect.yml` — pg_cron이 15분마다 호출, 각 실행은 55분 내부 루프
- 배포 `.github/workflows/pages.yml` — pg_cron이 매시 5분 호출 + 관련 코드 push 시

### 파일

```
src/api.ts         Neople API 클라이언트 (응답 타입 = 실측으로 확정한 것)
src/db.ts          Postgres 풀 + tx 헬퍼        ← DB를 바꾸려면 여기만
src/collect.ts     아이템 1개 수집 1회분 (핵심 로직)
src/run.ts         진입점 (상주 / --once / --minutes N)
src/forecast.ts    가격 예측 모델

scripts/init-db.ts        스키마 적용 + 화이트리스트 SEED (68종 하드코딩)
scripts/db-check.ts       연결 확인 (오류 원인을 한국어로 알려줌)
scripts/stats.ts          수집 현황 · 데이터 구멍 점검
scripts/probe.ts          특정 아이템의 거래 빈도 측정 → 폴링 주기 산정
scripts/discover.ts       아이템 카탈로그 눈덩이 크롤 → data/catalog.json
scripts/scan.ts           거래가 활발한 아이템 순위
scripts/scan-turnover.ts  스태커블 시장 24시간 거래대금 TOP100 (하루 1회)
scripts/track.ts          카탈로그에서 패턴으로 일괄 등록
scripts/legendary-floor.ts 레전더리 카드 165종 최저가 지수
scripts/migrate-sqlite.ts  (1회성, 이미 완료)

sql/schema.postgres.sql   정본 스키마
sql/scheduler.sql         pg_cron 트리거·시간봉 집계·보존 정책
web/build.ts              Supabase → dist/ 정적 사이트
web/index.html app.js style.css ranking.html ranking.js analysis.html guide.html
data/legendary-cards.json Actions용 레전더리 카드 165종 목록
data/market-items.json Actions용 스태커블 시장 후보 11,061종
certs/supabase-prod-ca-2021.crt Supabase pooler 인증서 검증용 공개 CA
```

### 명령

```bash
npm run db:check      # 연결 확인
npm run init          # 스키마 + SEED (여러 번 실행해도 안전)
npm run collect       # 상주 수집 (로컬)
npm run collect:once  # 전체 1회 (백필 겸용)
npm run stats         # 현황 점검
npm run probe -- "태초 소울 결정"
npm run probe -- --search 결정
node --env-file=.env --no-warnings web/build.ts   # 사이트 빌드
```

---

## 3. 반드시 알아야 할 함정

이것들 때문에 시간을 썼다. 모르고 건드리면 같은 데를 판다.

### API

- **`auction-sold`에 `auctionNo`가 없다.** 자연키로 중복을 제거해야 한다.
- **자연키만으로는 부족하다.** 대량 매수가 여러 매물을 한 번에 쓸어가면 *같은 초·같은 개당가·같은 수량인 별개의 체결*이 여러 건 생긴다. 실측: 레전더리 소울 결정은 응답 100건 중 **15건이 동일키**였다. `dup_seq`(응답 내 등장 순번)를 키에 포함하지 않으면 **거래량이 15% 과소집계**된다.
- **`regCount`는 `itemType: "스태커블"`인 매물에만 온다.** 아바타·장비 단품에는 아예 없다. 반드시 `regCount ?? count`로 받는다.
- **API 시각은 KST.** 저장은 ISO 8601 UTC로 통일 (`kstToIso`).
- 호출 제한은 초당 1,000건 — 우리 사용량(하루 1만 건 미만)에는 병목이 아니다.
- 아이템 아이콘은 `https://img-api.neople.co.kr/df/items/{itemId}` 로 공식 제공된다.
- 카드 옵션은 `/df/items/{itemId}`의 `cardInfo.enchant` 최대 업그레이드 단계에 있다.
  장비의 일반 상태값은 `itemStatus`이므로 아이템 종류별 응답 구조를 먼저 확인한다.

### 인프라

- **Supabase는 Session pooler로 붙는다.** Direct connection은 IPv6 전용인데 GitHub Actions 러너는 IPv4다. 비밀번호 특수문자는 percent-encode (`@`→`%40`, `!`→`%21`). Pooler의 사설 인증서 체인은 리포에 둔 Supabase Root 2021 CA로 검증한다.
- **Vercel Hobby의 Cron은 하루 1회만 된다.** 그래서 수집 스케줄러가 GitHub Actions다.
- **Actions 크론은 지연되거나 누락될 수 있다.** 15분 오프셋 크론으로 시작을 시도하고, 실행되면 55분간 내부 루프를 돌아 공백을 줄인다.
- **Postgres `numeric`은 pg가 문자열로 준다.** `EXTRACT(EPOCH ...)` 같은 건 `::float8`로 캐스팅해야 JS에서 숫자로 받는다.
- **Git Bash에서 curl에 한글 인자를 넘기면 깨진다** (MSYS가 native exe 인자를 재인코딩). 한글이 들어가는 API 호출은 반드시 Node 스크립트로.

### 수집 스케줄러 · 감시견

트리거와 감시가 모두 Supabase pg_cron 위에 있다. 등록 SQL은 `sql/scheduler.sql`, `sql/watchdog.sql`.

| cron job | 주기 | 하는 일 |
|---|---|---|
| `dnf-collect-dispatch` | `*/15` | GitHub `workflow_dispatch` 호출 → 수집 워크플로 기동 |
| `dnf-collect-watchdog` | `*/5` | 수집 공백 감시 · 자가복구 · 이슈 알림 |
| `dnf-events-dispatch` | 목요일 01:30 UTC | 공식 이벤트 동기화 |
| `dnf-candles-refresh` | 매시 2분 | 최근 48시간 원본 체결을 시간봉으로 upsert |
| `dnf-pages-dispatch` | 매시 5분 | 최신 DB로 정적 사이트 빌드·배포 |
| `dnf-health-prune` | 매일 04:17 | `collection_health` 90일 초과분 정리 |
| `dnf-runs-prune` | 매일 04:23 | `collection_runs` 7일 초과분 정리 |
| `dnf-deltas-prune` | 매일 04:29 | `listing_deltas` 14일 초과분 정리 |
| `dnf-trades-prune` | 매일 04:41 | 시간봉으로 집계된 30일 초과 원본 체결 정리 |

감시견 동작:
- **기록** — 매 점검마다 전역 공백과 각 아이템의 주기 대비 지연을 `collection_health`에 남긴다. 아이템은 자기 폴링 주기의 5배를 넘기면 stale이다
- **자가복구** — 공백 20분 초과 시 즉시 dispatch. 쿨다운 10분(없으면 워크플로가 계속 죽을 때 무한 재시도한다)
- **알림** — 40분 안에 복구 2회가 실패하면 GitHub Issue 생성. 쿨다운 6시간

**임계치 20분의 근거:** dispatch가 15분 주기이고 워크플로는 55분을 돈다. 실행 중이면 새 dispatch가 대기했다 이어받으므로 공백은 보통 몇 분 이내다. 20분을 넘겼다면 한 사이클이 통째로 유실된 것이다.

**알고 쓸 한계:** 감시견이 pg_cron 위에 있으므로 **pg_cron이나 Supabase가 죽으면 감시견도 같이 죽는다.** 감시 대상과 운명을 공유한다. 그 경우까지 잡으려면 인프라 밖에 별도 관찰자가 필요하다.

가동률 확인:
```sql
SELECT ROUND(100.0 * COUNT(*) FILTER (WHERE action IN ('ok','stale_items')) / COUNT(*), 2) AS global_uptime_pct
FROM collection_health WHERE checked_at > now() - interval '24 hours';
```

### 데이터

- **백필에는 시한이 있다.** 거래가 드문 아이템은 1개월치가 통째로 들어오지만 **한 달이 지나면 그 구간은 영원히 사라진다.** 이벤트 관련 아이템은 이벤트 시작 후 한 달 안에 받아야 한다.
- **아바타는 같은 `itemId`라도 같은 상품이 아니다.** `avatar.emblems`와 `ability`가 매물마다 달라 가격이 흩어진다. 장비의 강화 수치와 같은 문제.
- **잡템을 섞으면 분석이 뒤집힌다.** 8골드짜리 헝겊 조각과 300만 골드짜리 마법석을 같은 무게로 평균 내면 요일 효과가 전혀 다른 그림으로 나온다. 실제로 한 번 겪었다.

### 약관 (Neople)

- **제6조 ③** 출처 표기 의무 → 푸터에 있음. 지우지 말 것
- **제5조 ③** 대가 수취 금지 → 광고·후원 넣지 않는다
- **제5조 ⑧** 과부하 유발 금지 → 현재 사용량은 제한의 0.01% 수준

### 외부 사이트

- 종목 선정에 [dnfnow.xyz](https://dnfnow.xyz)의 "24시간 거래액 Top 20"과 [dnf-power.com](https://dnf-power.com/enchant-search)의 부위별 종결 목록을 **참고**했다.
- **시세 데이터는 전부 Neople API로 직접 받는다.** dnfnow의 `robots.txt`가 `/api/`를 금지하고 있다. 화면에는 출처를 표기하지 않지만 코드 주석에는 남겨뒀다 — 나중에 종결이 바뀌었을 때 어디서 확인했는지 알아야 한다.

---

## 4. 데이터로 답한 것 (분석 페이지)

분석 페이지는 "아이템 가치가 언제 오르고 내리는지 판단할 수 있는가"를 순서대로 묻는 구조다.
답하지 못한 질문은 왜 못 하는지와 언제 답할 수 있는지를 화면에서 함께 밝힌다.

### ① 가격은 랜덤워크인가 → 단기 신호를 탐색 중

**2026-09-08 리뷰에서 기존 결론을 정정했다.** 무거래 시간을 건너뛴 시계열을 1시간 간격으로
취급했고, 동분산 가정의 유의성과 VR만으로 가격의 평균회귀·매수 유리함까지 단정했다.

이제 종목별 가장 긴 연속 완료 시간봉 구간 하나를 쓰며, 같은 길이면 최신 구간을 선택한다.
최소 30봉이고 진행 중인 봉은 제외한다. Lo–MacKinlay의 겹치는 수익률 편향 보정과
이분산 견고한 표준오차를 적용하고, 양측 p값을 검정 가능한 종목 전체에 Holm 보정한다.
`src/market-logic.ts`의 `longestCompleteHours()`·`varianceRatio()`·`holmAdjusted()`가 계산한다.
제외 종목·이유, 선택 기간·관측 수, 원래 p값과 보정 p값을 화면에 공개한다.

2026-09-08 23:39 KST 로컬 빌드에서는 후보 34종 중 11종이 검정 가능했고,
VR 중앙값 0.703, 보정 후 반전 신호 1종·지속 신호 0종이었다. 수치는 빌드마다 달라진다.
봉당 체결 수 그룹 비교는 보조 자료이며, 잡음을 배제하는 검정으로 해석하지 않는다.
장기 평균회귀·회귀 계수·매수 전략의 유용성은 미검증이다. q=2만 점검하며 q=4·8은 후속 과제다.

### ② 요일에 따라 가격이 다른가 → **절을 내렸다** (표본 부족)

주기가 7일인 계절성이다. 2026-09-08 개편에서 **분석 페이지에서 제거했다.**
조건(이력 14일 초과·체결 25건 이상)을 충족한 종목이 4종뿐이고, 그중 3종이 같은 계열이며,
검정력 80% 기준 최소 탐지 가능 효과가 ±4.0%인데 관측된 차이는 전부 ±1.6% 안이다.
**사실상 아무것도 판별할 수 없는 상태라 절을 유지하는 것 자체가 오해를 부른다.**

계산은 `web/build.ts`에 그대로 남아 있고(`weekday`, `weekdaySample`) 예측 모델이 요일 계수를
쓰므로 지우면 안 된다. 상세 화면의 아이템별 요일 프로파일(완료 일봉 31일 필요)도 그대로다.
**종목이 늘면 절을 다시 올린다.** 지금 막고 있는 것은 기간이 아니라 종목 수이며,
100건 상한 탓에 30종이 `>14일 이력`에서 떨어진다. 09-21쯤 자동으로 풀린다.

같은 이유로 **아이템 유형 군집 분석도 만들었다가 내렸다.** 유동성×변동성 산점도에서
카테고리가 실제로 갈리는 것은 확인했지만(재료·소모품 봉당 110건 / 소울 결정 변동성 7.26% /
패키지 1.04%), 34종은 군집이 안정적이지 않다. 한계 절에 "왜 아직 못 내는지"만 남겼다.

### ③ 패키지 가격은 효율적으로 매겨지는가 → 그렇다

숲속의 유랑악단 패키지를 뜯으면 5개 상자가 나온다. 구성품 5종과 패키지의 최근 24시간 VWAP이
모두 있을 때만 차익을 계산한다. 수수료 3% 반영 후 사실상 0이라 무차익 조건의 교과서적 사례다.

**출시 충격은 영영 못 본다.** 출시 2026-08-27, 관측 시작 2026-09-06으로 10일 늦었고
이벤트 스터디는 t=0 전후를 모두 봐야 성립한다. **판매 종료 2026-11-05는 예고된 날이라
전후를 모두 관측할 수 있다** — 이 프로젝트가 준비 중인 이벤트다.

### ④ 가격을 예측할 수 있는가 → 날짜별 롤링 평가 중

`log(가격) = 추세 + 요일효과 + 잔차`, 구간은 잔차 10/90 분위수, 검증은 롤링 오리진 백테스트.
평가 날짜를 하루씩 이동하고 당일을 제외한 완료 일봉으로 요일 계수·추세·잔차를 다시 추정한다.
다른 종목의 자료와 학습 자격(완료 일봉 10개·학습 기간 일평균 체결 5건)도 과거 시점으로 제한한다.
공통 요일 계수의 자격은 그 시점의 이력 14일 초과·체결 25건이다. 해당 종목이 없으면 계수는 0이다.
평가일로부터 정확히 1일·7일 뒤에 완료 일봉이 있을 때만 평가한다. 당일을 제외하므로
1일 뒤 예측도 마지막 관측일에서는 최소 2일 뒤다. 현재 예측 제공 종목의 평가를 건수 가중으로
요약하고 거리별 종목 수·평가 건수·MAPE·naive·커버리지를 표시한다.
백필을 포함한 체결 시각 기준 평가이며, 당시 수집기가 실제 보유했던 데이터의 재현은 아니다.
성과와 실패 원인은 구분하며 분산비를 모델 실패의 원인으로 단정하지 않는다.

시간봉 실험(`scripts/experiment-hourly-forecast.ts`)의 2026-09-08 기록은 MAPE 3.20% vs
naive 2.81%, 34종 중 10종 승리다. 단일 홀드아웃 실험이며 같은 구간에서 폭 후보를 고른
결과라 현재 롤링 평가와 직접 비교하지 않는다. 원인이 잡음이라고 확정하지 않고 일봉 운영을 유지한다.

### 거래량은 하한값이다 (절이 아니라 §05 한계로)

API 관측 거래량은 100건 상한 때문에 과소집계된다. `dup_seq`로 동일키 손실은 막았지만
초과분은 알 수 없다. `regCount − count` 기반 매물 소진량은 판매·취소가 섞이고 API 체결과
중복될 수 있어 합산하지 않는다. **한 절을 쓸 만큼의 결과가 아니라 모든 절에 걸리는 제약이라
2026-09-08 개편에서 한계 절로 옮겼다.**

### 부수 발견

종결 칭호의 최종 데미지가 갈린다 — 종이달 오르골 9.6% vs 나머지 3종 8%. **가격 순서가 스탯 순서와 정확히 일치**하며, 시장이 1.6%p를 약 870만 골드로 값매김하고 있다.

### 거래대금 TOP100 시험 집계

카탈로그에서 옵션 혼합이 없는 스태커블 11,061종을 하루 한 번 훑는다. 최근 100건이
24시간을 모두 덮으면 그대로 합산하고, 덮지 못하면 관측 시간당 거래대금을 24시간으로
환산해 `24h 환산`으로 표시한다. 기존 추적 종목은 DB의 최근 24시간 연속수집 합계로
대체한다. 최신 100종만 `market_rankings`에 저장하며 7일이 지난 집계는 지운다.
장비·아바타·카드는 옵션이나 업그레이드 단계가 섞이므로 전수 탐색에서 제외한다.

---

## 5. 다음 작업

> 리뷰에서 나온 구체적인 할 일은 **[TASKS.md](TASKS.md)** 에 우선순위대로 정리돼 있다.


### P0. 수집 안정화 ← 코드 완료, 배포와 상시 실행 환경 선택 필요

- **55분 연속 수집** — 완료. pg_cron이 Actions를 15분마다 호출하고, 작업은 55분·타임아웃 65분이다.
- **재시작 스케줄 복원** — 완료. DB의 마지막 성공 시각에서 `nextAt`을 복원하고 매 건마다 가장 급한 아이템을 다시 고른다. 실패는 60초 뒤 재시도한다.
- **고빈도 분리** — 완료. Actions matrix에서 300초 이하 `hot` 8종과 `rest` 60종을 별도 작업으로 실행한다. 현재 예상 호출량은 각각 시간당 114회·112회다.
- **10분 공백 경고** — 완료. `npm run health`가 마지막 성공 수집을 확인하며, Codex의 `던파 수집 10분 감시`가 이상일 때만 알린다.
- **수집 출처 기록** — 완료. 새 실행은 `local` / `manual` / `actions`로 기록한다. 과거 행은 추측하지 않고 `NULL`로 보존한다.
- **포화 시 자동 단축** — 완료. 100건 응답이 이전 수집과 겹치지 않고 그 시간 범위가 현재 주기의 1.5배보다 짧을 때만 다음 단계로 줄이며 최소 60초에서 멈춘다.
- **상시 실행 환경** — 미완료. GitHub 크론과 독립적인 호스트가 필요하며 비용·계정이 걸리므로 배포 대상을 먼저 정해야 한다.

### P1. 거래량 표현 ← 완료, 배포 필요

- API 체결에서 센 값은 **API 관측 거래량**, `listing_deltas`는 **매물 소진 추정량**으로 분리한다.
- 판매·취소 및 API 체결과의 중복을 판별할 수 없어 두 값을 합산하지 않는다.
- “상당 부분 복원”, “포화면 누락 확정” 같은 과한 표현을 제거했다.

### A. 아이템 상세 그래프 확장 ← A-1·A-2·A-3·A-4·A-5·A-6 완료

현재 중요도순으로 가격·예측 / 아이템별 요일 프로파일 / 호가–체결 갭 / 가격 × 거래량 4분면 / 매물 사다리 / 매물 소진 속도 / 시간별 VWAP / 일별 수량을 그린다. 매물 사다리는 대량 구매 의미가 있는 재료·소모품과 소울 결정에만 표시한다. 완료 내역:

1. **호가–체결 갭** — **완료 (2026-09-07).** 최근 7일의 각 호가 스냅샷에서 `(최저 호가 ÷ 직전 24시간 VWAP) − 1`을 계산한다. 직전 24시간에 체결이 없으면 제외하고, 같은 초의 스냅샷은 마지막 관측 하나만 쓴다.
2. **매물 사다리(호가 깊이)** — **완료 (2026-09-07).** 재료·소모품과 소울 결정의 열린 매물을 가격대별로 합쳐 최대 100개까지 계단 그래프로 표시하고, 10개·100개 구매 평균을 계산한다. 수량이 모자라면 추정하지 않고 매물 부족으로 표시한다.
3. **가격 × 거래량 4분면** — **완료 (2026-09-07).** 최근 최대 30개 완료 일봉을 각 아이템의 평균 가격·평균 수량으로 정규화해 수요 증가/감소와 공급 증가/감소를 판별한다. 당일 미완성 거래량은 제외하고 완료 일봉 3일 이상부터 표시한다.
4. **매물 소진 속도 — 완료 (2026-09-07).** 최근 7일의 관측 소진량을 직전 매물 스냅샷부터의 실제 경과시간으로 나눠 시간당 속도로 환산한다. 수량 감소와 만료 전 소멸을 따로 집계하고, 판매·취소를 구분할 수 없는 한계를 화면에 밝힌다. 수집이 없던 시간은 0이 아니라 공백으로 표시한다.
5. **아이템별 요일 프로파일 — 완료 (2026-09-08).** 완료 일봉 이력이 31일 이상이고 요일별 표본이 3일 이상인 아이템만 가격·API 관측 수량을 각 아이템 평균 100의 지수로 표시한다. 패치·출시·종료 전후 3일은 제외하고 목요일을 강조한다. 표본이 부족하면 남은 이력과 요일별 표본 수를 안내한다.
6. **캔들(OHLC) — 완료 (2026-09-08).** 시가·고가·저가·종가 캔들 위에 일별 VWAP 실선을 겹쳐 표시한다. 상승일은 빨강, 하락일은 파랑이며 VWAP 예측 점선은 마지막 실측 VWAP에서 이어진다. 캔들은 버튼으로 켜고 끌 수 있고 선택은 브라우저에 저장한다. 이벤트 태그를 유지하고 당일 봉은 수집 중인 미완성 값임을 화면에 밝힌다.

D등급(일평균 체결 5건 미만)은 표본 신뢰도가 낮으므로 가격 이력은 보여주되 예측은 생성하지 않는다.

### B. 남은 것

- **카드 34종 `key_stat` — 완료 (2026-09-08).** `/df/items/{itemId}`의 `cardInfo.enchant` 최대 업그레이드 옵션에서 읽었다. 카드 목록과 상세에 핵심 스탯을 표시한다.
- **카드 34종 `final_since` — 완료 (2026-09-08).** 각 카드가 최초 등장한 콘텐츠의 공식 라이브 서버 적용일을 시드와 DB에 기록했다. 목록과 상세의 `종결 D+`는 이 날짜를 기준으로 계산한다.
- **이벤트 스터디 기반 — 완료 (2026-09-07).** 관련 아이템 가격 그래프의 날짜축 아래에는 `퍼스트서버/대규모/주요/패키지 출시` 네 종류의 작은 태그만 표시하고, 일반 정기점검은 제외한다. 공지일은 분석하지 않고 실제 시장 충격일 하나만 쓴다. 상세에서 퍼스트서버는 패치일, 대규모·주요는 적용일, 패키지는 출시일과 종료일을 보여 준다. 전체 이벤트는 적용일이 해당 아이템의 첫·마지막 일봉 사이에 있을 때만 연결하고, 패키지처럼 `related_item_ids`로 직접 연결된 이벤트는 유지한다. 같은 날짜에 종류가 겹치면 나란히 묶고, 이벤트 상세는 기본으로 접어 둔다. 화살표나 태그를 누르면 분석값을 펼친다. 완료 일봉이 전후 3일씩 있으면 전체 시장 요일효과를 보정한 VWAP·API 관측 수량 변화를 계산한다. 매주 목요일 공식 공지와 추적 아이템 이름이 맞는 세리아 상점 패키지를 동기화한다.
- **강화·증폭은 종결로 표시하지 않는다 (2026-09-08 사용자 결정).** `+12 장비 증폭권[골고라이언]`보다 높은 증폭 단계가 있으므로 추적 목록 안에서 가장 높다는 이유로 종결을 붙이지 않는다.
- **이벤트 동기화 견고성 — 완료 (2026-09-08).** 공지 다음 날로 계산한 적용일은 `note`에 추정임을 남긴다. 상세 공지 하나가 실패해도 나머지는 계속 처리하며, 업데이트·패키지 후보가 0건이면 HTML 구조 확인 경고를 출력한다.
- **모바일 화면 — 완료 (2026-09-08).** 780px 이하에서는 목록을 이름·현재가·24h 세 열로 줄이고, 요약 카드를 한 줄 가로 스크롤로 표시한다. 헤더 줄바꿈을 막고 탭의 오른쪽 그룹에 `종결 슬롯` 라벨을 붙였다. 준비되지 않은 요일 패널은 상세 맨 아래로 이동하며 변동률이 없으면 `데이터 없음`으로 표시한다.
- **API 키 재발급 — 진행하지 않음 (사용자 결정, 2026-09-07).**

### C. 달력에 박아둘 날짜

- **2026-11-05** 유랑악단 패키지 판매 종료. 분석 페이지가 계산하는 해체 마진이 이때 깨지는지 보는 것이 이벤트 스터디의 본 소재.
- **종결 교체** 크리쳐·칭호가 7개월 넘게 종결 유지 중(D+228). 교체가 일어나면 그것도 이벤트 스터디 소재.

---

## 6. 환경 변수

`.env` (gitignore됨):

```
NEOPLE_API_KEY=<developers.neople.co.kr에서 발급>
DATABASE_URL=postgresql://postgres.<ref>:<비밀번호>@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres
```

GitHub Actions Secrets에 같은 두 개가 등록되어 있다.

**API 키를 클라이언트 번들에 절대 넣지 않는다.** 정적 사이트는 빌드 시점에 DB에서 읽은 결과만 담고, 브라우저는 Neople API를 직접 호출하지 않는다.

---

## 7. 코드 스타일

- 주석은 **왜**를 쓴다. 무엇을 하는지는 코드가 말한다.
- 한국어 주석. 실측으로 확인한 수치는 근거로 남긴다 (예: "실측: 100건 중 15건이 동일키").
- 데이터의 한계를 숨기지 않는다. 표본이 얇으면 화면에 그렇게 표시한다(유동성 등급, `?` 표시, 커버리지).
- 등락 색은 한국 시장 관행 — **상승 빨강, 하락 파랑**.
